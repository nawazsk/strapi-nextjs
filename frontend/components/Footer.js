import Link from 'next/link';
import css from 'styled-jsx/css';

const styles = css`
  .container {
      margin:0;
      padding:0;
  }
`;

const Footer = () => (
    <div>
        Footer
    </div>
)

export default Footer;